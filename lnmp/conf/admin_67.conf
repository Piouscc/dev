#core
server{
	listen	80;
	server_name	#core#;
	index	index.html index.htm index.php;
	root	/www/67/core;
	
	#listen 443 ssl;
	#ssl_certificate 67/#domain#.crt;
	#ssl_certificate_key 67/#domain#.key;
	#ssl_session_timeout 5m;
	#ssl_protocols TLSv1.1 TLSv1.2;
	#ssl_ciphers ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;
	#ssl_prefer_server_ciphers on;
	
	#if ($scheme != 'https') {
	#	rewrite ^/.* https://$host/$1 permanent;
	#}
	
	location ^~ /config/ {
		return 403;
	}
	location ^~ /data/ {
		return 403;
	}
	location ^~ /cache/ {
		return 403;
	}
	location ^~ /controllers/ {
		return 403;
	}
	location ^~ /view/ {
		return 403;
	}
	
	location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|woff|woff2|svg|ttf|eot|ico)$ {
		expires	30d;
		access_log	off;
	}
	
	location ~ \.php {
		include fastcgi.conf;
		fastcgi_pass	127.0.0.1:9000;
		fastcgi_index	index.php;
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
	}
	
	if (!-e $request_filename) {
		rewrite ^/(.*) /index.php/$1 last;
	}
	
	access_log	/var/#NAME#/log/core.nginx.log access;
}

#platform admin
server{
	listen	80;
	server_name	#plf_admin#;
	index	index.html index.htm index.php;
	root	/www/67/platform/admin;
	
	#listen 443 ssl;
	#ssl_certificate 67/#domain#.crt;
	#ssl_certificate_key 67/#domain#.key;
	#ssl_session_timeout 5m;
	#ssl_protocols TLSv1.1 TLSv1.2;
	#ssl_ciphers ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;
	#ssl_prefer_server_ciphers on;
	
	#if ($scheme != 'https') {
	#	rewrite ^/.* https://$host/$1 permanent;
	#}
	
	location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|woff|woff2|svg|ttf|eot|ico)$ {
		expires 30d;
		access_log	off;
	}
	
	location ^~ /controllers/ {
		return 403;
	}
	location ^~ /view/ {
		return 403;
	}
	
	location ~ \.php {
		include fastcgi.conf;
		fastcgi_pass	127.0.0.1:9000;
		fastcgi_index	index.php;
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
	}
	
	if (!-e $request_filename) {
		rewrite ^/(.*) /index.php/$1 last;
	}
	
	access_log	/var/#NAME#/log/admin.nginx.log access;
}


#stat
server{
	listen	80;
	server_name #stat#;
	index index.html index.htm index.php;
	root	/www/67/stat;
	
	#listen 443 ssl;
	#ssl_certificate 67/#domain#.crt;
	#ssl_certificate_key 67/#domain#.key;
	#ssl_session_timeout 5m;
	#ssl_protocols TLSv1.1 TLSv1.2;
	#ssl_ciphers ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;
	#ssl_prefer_server_ciphers on;
	
	#if ($scheme != 'https') {
	#	rewrite ^/.* https://$host/$1 permanent;
	#}

	location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|woff|woff2|svg|ttf|eot|ico)$ {
		expires	30d;
		access_log	off;
	}
	
	location ^~ /data2/ {
		return 403;
	}
	location ^~ /controllers/ {
		return 403;
	}
	location ^~ /view/ {
		return 403;
	}
	
	location ~ \.php {
		include fastcgi.conf;
		fastcgi_pass	127.0.0.1:9000;
		fastcgi_index	index.php;
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
	}
	
	if (!-e $request_filename) {
		rewrite ^/(.*) /index.php/$1 last;
	}
	
	access_log	/var/#NAME#/log/stat.nginx.log	access;
}


#ad admin
server{
	listen	80;
	server_name	#adv_admin#;
	index	index.html index.htm index.php;
	root	/www/67/adv/admin;
	
	#listen 443 ssl;
	#ssl_certificate 67/#domain#.crt;
	#ssl_certificate_key 67/#domain#.key;
	#ssl_session_timeout 5m;
	#ssl_protocols TLSv1.1 TLSv1.2;
	#ssl_ciphers ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;
	#ssl_prefer_server_ciphers on;
	
	#if ($scheme != 'https') {
	#	rewrite ^/.* https://$host/$1 permanent;
	#}

	location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|woff|woff2|svg|ttf|eot|ico)$ {
		expires 30d;
		access_log	off;
	}
	
	location ^~ /controllers/ {
		return 403;
	}
	location ^~ /view/ {
		return 403;
	}

	location ~ \.php {
		include fastcgi.conf;
		fastcgi_pass	127.0.0.1:9000;
		fastcgi_index	index.php;
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
	}

	if (!-e $request_filename) {
		rewrite ^/(.*) /index.php/$1 last;
	}

	access_log	/var/#NAME#/log/adv.admin.nginx.log access;
}


#cp
server{
	listen	80;
	server_name	#cp#;
	index	index.html index.htm index.php;
	root	/www/67/cp;
	
	#listen 443 ssl;
	#ssl_certificate 67/#domain#.crt;
	#ssl_certificate_key 67/#domain#.key;
	#ssl_session_timeout 5m;
	#ssl_protocols TLSv1.1 TLSv1.2;
	#ssl_ciphers ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;
	#ssl_prefer_server_ciphers on;
	
	#if ($scheme != 'https') {
	#	rewrite ^/.* https://$host/$1 permanent;
	#}
	
	location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|js|css|woff|woff2|svg|ttf|eot|ico)$ {
		expires 30d;
		access_log	off;
	}
	
	location ^~ /controllers/ {
		return 403;
	}
	location ^~ /view/ {
		return 403;
	}

	location ~ \.php {
		#limit_req zone=php burst=60 nodelay;
		
		include fastcgi.conf;
		fastcgi_pass	127.0.0.1:9000;
		fastcgi_index	index.php;
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
	}
	
	if (!-e $request_filename) {
		rewrite ^/(.*) /index.php/$1 last;
	}

	access_log	/var/#NAME#/log/cp.nginx.log access;
}